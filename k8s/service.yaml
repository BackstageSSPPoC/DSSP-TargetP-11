
apiVersion: v1
kind: Service

metadata:
  name: DSSP-TargetP-11
  namespace: default

spec:
  selector:
    app: DSSP-TargetP-11

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: ClusterIP
